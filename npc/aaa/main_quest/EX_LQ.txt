//--------------------------------------------------
// NPC: Ksa - Icecastle (เริ่มเควส)
//--------------------------------------------------

icecastle,141,216,5	script	Ksa#Ice	531,{
	//lighthalzen,155,257,5	script	Ksa#Ice	531,{
	if (step>=2) {
		mes "[Ksa]";
		mes "เวลาไม่คอยท่า!!ไปพบหัวหน้า Lived ที่ห้องวิจัยในเมือง Lighthalzen!!";
		if (select("ไปเลย:ยังไม่ไป") == 1) {
			warp "lighthalzen",156,252;
			end;
		}
		end;
	}
	if (step == 1){
		switch (isbegin_quest(105)){
			case 0: 
				mes "[Ksa]";
				mes "มันไม่น่าจะเกิดขึ้นได้นะ ฉันลืมให้เควสเจ้าหรอ....";
				setquest 105;
				end;
				
			case 1:
				switch (checkquest(105,HUNTING)){
					case 0: break;
					case 1: //เควสยังไม่เสร็จ
						mes "[Ksa]";
						mes "อืมมมม....มันต้องหนีไปอยู่แถวๆรอบๆเมืองแน่เลย....";
						break;
					case 2: //เควสเสร็จ ไปหา Lived
						mes "[Ksa]";
						mes "ข้าคิดอยู่แล้วว่าแผนนี้จะได้ผล.... ";
						mes "อ๊ะ ท่านนักผจญภัย ขออภัยที่หลอกลวงท่าน แต่แผนข้านี่มันฉลาดล้ำจริงๆ ไม่โกรธกันนะ";
						mes "นี่รางวัลของท่าน";
						getitem 2002002,1;
						completequest 105;
						set step,2;
						next;
						mes "[Ksa]";
						mes "ข้าแค่อยากได้นักผจญภัยที่แข็งแกร่งแบบท่านมาช่วยงานวิจัยของท่าน Lived เท่านั้นเอง!!";
						mes "ซึ่งเจ้าก็ผ่าน!! ผ่าน ผ่าน ผ่าน 3 ผ่านไปเลย!!";
						mes "อยากจะมาช่วยงานของพวกข้าไหมหละ ผลตอบแทนดีไม่น้อยเลยนา~~";
						next;
						mes "[Ksa]";
						mes "เจ้ามาที่ศูนย์วิจัยของพวกข้าที่ Lighthalzen นะ ข้าจะรอท่าน!!";
						next;
						mes "[Ksa]";
						mes "ต้องการไปที่ศูนย์วิจัยเลยหรือไม่?";
							if (select("ไปเลย:ยังไม่ไป") == 1) {
								warp "lighthalzen",156,252;
								end;
							}
						mes "[Ksa]";
						mes "ถ้าจะไปก็มาหาข้าได้เลย!!!";
						end;
				}
			case 2: break;
		}
	}
	if (step == 0 && BaseLevel > 100 && adv_clas >= 1) { 
		mes "[Ksa]";
		mes "โอ้พระเจ้า! ตัวทดลองของข้าหลุดออกระหว่างทำการทดลองแถวๆนี้!!";
		mes "ถ้าไม่จัดการ พวกมันจะสร้างความเสียหายใหญ่แน่ๆ!";
		next;
		mes "[Ksa]";
		mes "ช่วยข้าจัดการพวกมันทีได้ไหม?";
		if(select("ได้เลย:ไม่สนใจ")==2) {
			mes "[Ksa]";
			mes "เอ... ตามแผนข้ามันไม่น่าจะเป็นแบบนี้นี่หว่า.....";
			close;
		}
		mes "[Ksa]";
		mes "เยี่ยม! รีบไปจัดการพวกมันแล้วกลับมาหาข้า!";
		setquest 105;
		set step,1;
		close;
	}
	else {
		mes "[Ksa]";
		mes "นักผจญภัยระดับเจ้าไม่น่ารอด.....";
		next;
		mes "^FF0000ต้องการ LV มากกว่า 100 ";
		mes "^FF0000และ Rank E ขึ้นไป";
	}
	Oninit:
	waitingroom "EX.Story - Human Evolve",0;
	end;
}


//--------------------------------------------------
// NPC: Warp เข้า 1@lhz_d1
//--------------------------------------------------
lighthalzen,162,252,0	warp	To_Lived	1,1,1@lhz_d1,151,30
1@lhz_d1,151,27,0	warp	Back_Lived	1,1,lighthalzen,159,252

//--------------------------------------------------
// NPC: Lived - Research Leader (เริ่มเควสวัตถุดิบ)
//--------------------------------------------------
1@lhz_d1,161,54,3	script	Lived Leader	993,{ 
	if (step==9) { // จัดการตัวทดลองหลุดเสร็จ แล้วไปฆ่าบอสต่อ 
		mes "[Lived]";
		mes "ข้ายังไม่ว่างคุยกับท่าน....."; // แ
		end;
	}
	if (step==3) { //หลังคุยกับ Ksa ในแลป 
		mes "[Lived]";
		mes "ข้าดีใจที่เจ้ามาถึงศูนย์วิจัยของเรา!";
		mes "คุณคือนักผจญภัยที่ Ksa พูดถึงสินะ ข้าคือ Lived";
		mes "หัวหน้าของการวิจัยเพื่อสร้างสิ่งมีชีวิตที่ทำให้โลกสงบสุขได้...";
		next;
		mes "[Lived]";
		mes "นายไม่คิดหรอ ว่าประชากรของพวกเรานั้นอ่อนแอขนาดไหน";
		mes "แค่เจอสัตว์ป่าข้างทางก็แทบจะเอาชีวิตกันไม่รอดแล้ว ต้องให้พวกท่านนักผจญภัยช่วยเหลือ";
		next;
		mes "[Lived]";
		mes "มันดู.....ไร้ค่า มันดู....อ่อนแอที่แม้แต่ปกป้องตัวเองยังทำไม่ได้...";
		mes "เพราะงั้นช่วงเวลานี้แหละ!! ที่จะทำให้ความฝันของพวกเราเป็นจริง!!";
		mes "ไม่ต้องหวาดกลัวกับสัตว์ป่า หรืออสูรกายจากนอกเมือง!!";
		mes "เพราะเราจะมีสิ่งมีชีวิตที่ช่วยคุ้มครองเราตลอดเวลา!! ไม่ต้องหวาดกลัวกับอะไรทั้งนั้น!!";
		next;
		mes "[Lived]";
		mes "นี่คือความตั้งใจของพวกเราในการวิจัยครั้งนี้ และแน่นอน";
		mes "การที่ท่านนักผจญภัยมาช่วยเราในการวิจัยครั้งนี้ มันจะต้องสำเร็จอย่างแน่นอน!!!";
		next;
		mes "[Lived]";
		mes "อ้อ...ส่วนเรื่องเนื้อหาสำหรับการทดลองเพิ่มเติม ข้าขอให้ท่านไปคุยกับ Ksa เพื่อดำเนินการต่อนะ";
		mes "ข้าติดงานอยู่หนะ ถึงแม้จะคุยเพลินไปซักหน่อย......";
		set progress1,0;
		set progress2,0;
		set progress3,0;
		set step,4; // ไป Ksa Lab เพื่อฟังข้อมูล
		close;
	}
	if (step==6 && sideLQA == 1 && sideLQB == 1 && sideLQC == 1 && sideLQD == 1) { //หลังได้รับของทดลอง+ทำเควสกับนักวิทย์ทั้งหมดแล้ว เพิ่มเงื่อนไขช่วยนักวิทย์ทั้งหมด 2-3 คน+อ้วน 1 คน
		mes "[Lived]";
		mes "ทุกอย่าง.....พร้อมแล้ว!! มาเริ่มก้าวแรกของมนุษยชาติรุ่นใหม่กันดีกว่า!!";
		//เพิ่ม effect การทดลองนู่นนี่ ก่อนจะระเบิด ตู้มมม ทดลองล้มเหลว
		next;
		mes "[Lived]";
		mes "ไม่นะ! การทดลองล้มเหลว!! ตัวทดลองบางส่วนหลุดออกมา!!";
		set step,7; // mob หลุด
			monster "1@lhz_d1",143,42,"Chief Impostor",27005,1,0,"exsam_fight::OnKill";
			monster "1@lhz_d1",159,48,"Chief Impostor",27006,1,0,"exsam_fight::OnKill";
			monster "1@lhz_d1",148,55,"Chief Impostor",27007,1,0,"exsam_fight::OnKill";
			monster "1@lhz_d1",152,49,"Chief Impostor",27005,1,0,"exsam_fight::OnKill";
			monster "1@lhz_d1",154,38,"Chief Impostor",27006,1,0,"exsam_fight::OnKill";
			set @exsam_spawned, 1;
		close;
	}
	if (step==8) { // จัดการตัวทดลองหลุดเสร็จ แล้วไปฆ่าบอสต่อ 
		mes "[Lived]";
		mes "เสร็จแล้ว... แต่ตัวต้นแบบยังหนีไป!"; // แก้ text
		mes "เจ้าไปตามจัดการมันที่ 1@rgsr_d1 ได้เลย!"; // แก้ text
		setquest 106; // Prototype escape
		set step,9;
		close;
	}
	if (step == 9){ //หลังฆ่าบอสแมพจบ เพื่อเปิดไป instance 
		switch (isbegin_quest(106)){
			case 0: 
				mes "[Lived]";
				mes "มันไม่น่าจะเกิดขึ้นได้นะ ฉันลืมให้เควสเจ้าหรอ....";
				setquest 106;
				end;
				
			case 1:
				switch (checkquest(106,HUNTING)){
					case 0: break;
					case 1: //เควสยังไม่เสร็จ
						mes "[Lived]";
						mes "มันต้องหลบอยู่ในแลปวิจัยข้างใต้นี่แหละ....";
						break;
					case 2: //เควสเสร็จ ไปหา Lived
						mes "[Lived]";
						mes "เรื่องที่เกิดครั้งนี้ทำให้เราต้องนับหนึ่งใหม่อีกแล้วสินะ....";
						next;
						mes "[Lived]";
						mes "นี่คือรางวัลของท่านในการปกป้องเมืองนี้ และที่วิจัยที่นี่";
						getitem 2099002,1;
						next;
						mes "[Lived]";
						mes "แต่เรื่องนี้ยังไม่จบ...";
						mes "การวิจัยของข้ามันยังไม่จบ!!!";
						completequest 106;
						set step,9;
						
						break;
					default:
						mes "Error Please contract Admin";
						end;
				}
			case 2: break;
		}
	}
	else {
		mes "[Lived]";
		mes "อย่าพึ่งมายุ่งกับข้า...";
		end;
	}
	end;
}

-	script	exsam_fight	-1,{
	OnKill:
		if ((killedrid == 27005 || killedrid == 27006 || killedrid == 27007) && step == 7) {
			set #exsam_kill, #exsam_kill + 1;
			if (#exsam_kill >= 5) {
				set step,8; //เคลียมอนครบแล้ว กลับไปคุย Lived เพื่อไปตี Prototype
				set #exsam_kill, 0;
				set @exsam_spawned, 0;
			}
		}
	end;
}

//--------------------------------------------------
// NPC: Ksa - 1@lhz_d1 (เตรียมวัตถุดิบ)
//--------------------------------------------------
1@lhz_d1,143,54,5	script	Ksa#Lab	531,{
	if (step == 6 ) {
		mes "[Ksa]";
		mes "ไปช่วยลูกน้องของข้าทำงานวิจัยไปก่อนนะ!!";
		mes "รวมถึงไอ้อ้วนนั้นด้วย ถ้ามันอยากได้อะไรก็ให้มันไปซะ!!";
	end;
	}
	if (step == 2 ) {
		mes "[Ksa]";
		mes "เจ้ามาตรงเวลาที่ข้าคำนวณไว้เป๊ะ เหมือนกับการทดลองของข้าเลย!!";
		mes "ข้านี่มันอัจฉริยะจริงๆ ข้าขอต้อนรับสู่ห้องวิจัยของท่าน Lived!!";
		next;
		mes "[Ksa]";
		mes "คนที่ยืนอยู่ตรงนั้นคือท่าน Lived ผู้จำในการวิจัยครั้งนี้..";
		mes "เพื่อการพัฒนาของมนุษยชาติให้ก้าวล้ำขึ้นไปอีก!!";
		next;
		mes "[Ksa]";
		mes "(กระซิบ) ส่วนไอ้หมูตอนข้างๆ คือนักลงทุนหลักของงานนี้...";
		mes "ไอ้หมูนั้นคือบ่อเงินบ่อทองของงานนี้ แต่จริงๆมันสนใจแต่ชื่อเสียงมันแค่นั้นแหละ";
		mes "ไอ้หมูโลภมากเอ้ย...";
		next;
		mes "[Ksa]";
		mes "เอาหละ จบการต้อนรับแล้ว!! ข้าต้องไปทำงานต่อละ...";
		mes "ข้าแนะนำเจ้าคร่าวๆกับท่าน Lived แล้ว!! เข้าไปคุยกับเขาสิ เขารออยู่.....";
		set step,3; //คุย Ksa เสร็จ ไปหา Lived
	end;
	}
	if (step == 4 ) {
		mes "[Ksa]";
		mes "เจ้าคุยกับท่าน Lived เรียบร้อยแล้วมาหาข้า แปลว่าเจ้าสนใจการวิจัยครั้งนี้สินะ!!";
		mes "ดี!! มาเป็นลูกมือข้าซะดีๆ ฮาๆๆ";
		next;
		mes "[Ksa]";
		mes "ข้าต้องการวัตถุดิบจากเจ้า 3 อย่าง:";
		mes "1. Yggdrasil Berry 5 ea";
		mes "2. Temporal Crystal 1 ea";
		mes "3. Broken Horn 1 ea";
		next;
		mes "[Ksa]";
		mes "แน่นอน เมื่อเจ้าของวัตถุดิบมา พวกข้าก็ต้องตรวจสอบมันด้วย!!";
		mes "ว่ามันสามารถใช้งานในการวิจัยได้ไหม......";
		next;
		mes "[Ksa]";
		mes "ซึ่งมีวิธีที่ง่ายกว่านั้น.........";
		next;
		mes "[Ksa]";
		mes "เงินไงหละ!!";
		mes "เจ้าสามารถให้เงินเพื่อที่พวกข้าจะไปซื้อของจากตลาดมืดได้!!";
		mes "ด้วยเงินทุนที่เพิ่มมา จะทำให้เราได้ของในแบบที่ต้องการเลยหละ!!";
		next;
		mes "[Ksa]";
		mes "เอาหละ.... มาเริ่มงานวิจัยแรกของท่านกันดีกว่า......";
		set step,5; //รับภารกิจหาของวิจัย ไม่ก็เงิน
	end;
	}
	if (step == 5) {
		// Step 1: Yggdrasil Berry
		if (progress1 == 0) {
			mes "[Ksa]";
			mes "ขั้นแรก ต้องใช้ Yggdrasil Berry 5 ea หรือไม่ก็เอามาให้ข้า 5,000,000 Zeny";
			next;
			switch(select("ส่งวัตถุดิบ","ลงทบทุนวิจัย","ยกเลิก")) {
				case 1:
					if (countitem(607) >= 5) {
						progressbar "0xFF0000",3;
						if (rand(100) < 5) {
							delitem 607,5;
							mes "[Ksa]";
							mes "โอเค วัตถุดิบแรกสมบูรณ์พร้อมใช้ ไปกันต่อ!!";
							set progress1,1;
						} else {
							mes "[Ksa]";
							mes "ใช้ไม่ได้!! ไปหามาใหม่!!!";
						}
					} else {
						mes "[Ksa]";
						mes "เจ้านับเลขเป็นป่ะเนี่ยย มันครบจำนวนไหม?!?!";
					}
					close;
				case 2:
					if (Zeny >= 5000000) {
						mes "[Ksa]";
						mes "ไหนขอนับหน่อยซิ.....";
						next;
						progressbar "0xFF0000",3;
						Zeny -= 5000000;
						mes "[Ksa]";
						mes "เยี่ยม ข้าจะไปซื้อมาเดี๋ยวนี้แหละ!";
						set progress1,1;
					} else {
						mes "[Ksa]";
						mes "เจ้าตกเลขรึไง?!?! มันไม่ครบ!!!";
					}
					close;
				case 3:
					close;
			}
		}

		// Step 2: Temporal Crystal
		if (progress2 == 0 && progress1 == 1) {
			mes "[Ksa]";
			mes "ขั้นต่อมา ต้องใช้ Temporal Crystal 1 ea หรือไม่ก็เพิ่มงบวิจัยมาอีก 15,000,000 Zeny";
			next;
			switch(select("ส่งวัตถุดิบ","ลงทุนอีก!!","ยกเลิก")) {
				case 1:
					if (countitem(6607) >= 1) {
						progressbar "0xFF0000",3;
						if (rand(100) < 5) {
							delitem 6607,1;
							mes "[Ksa]";
							mes "ยอดเยี่ยม วัตถุดิบสมบูรณ์สุดๆ! ได้เวลาหาของชิ้นต่อไปแล้ว!!";
							set progress2,1;
						} else {
							mes "[Ksa]";
							mes "นี่มันห่วยแตก!! เจ้าไม่เห็นรอยร้าวตรงนี้รึไง?!? ไปหามาใหม่!!!";
						}
					} else {
						mes "[Ksa]";
						mes "สมองเจ้าลอยไปกับ Poring แล้วรึไง!! มันไม่ครบ!!";
					}
					close;
				case 2:
					if (Zeny >= 15000000) {
						mes "[Ksa]";
						mes "ไหนขอนับหน่อยซิ.....";
						next;
						progressbar "0xFF0000",3;
						Zeny -= 15000000;
						mes "[Ksa]";
						mes "ยอดเยี่ยม เจ้านี่มันเงินถุงเงินถังของข้าจริงๆ!!";
						set progress2,1;
					} else {
						mes "[Ksa]";
						mes "นี่มันนักผจญภัยหรือ Poring เนี่ย! เงินมันไม่พอ!!";
					}
					close;
				case 3:
					close;
			}
		}

		// Step 3: Broken Horn
		if (progress3 == 0 && progress2 == 1) {
			mes "[Ksa]";
			mes "ขั้นสุดท้าย!! ต้องใช้ Broken Horn 1 ea หรือลงทุนให้กับพวกข้า 30,000,000 Zeny!!";
			next;
			switch(select("ส่งมอบวัตถุดิบ","เอาเงินไปเลย!!","ยกเลิก")) {
				case 1:
					if (countitem(6649) >= 1) {
						progressbar "0xFF0000",3;
						if (rand(100) < 5) {
							delitem 6649,1;
							mes "[Ksa]";
							mes "เขานี่มัน!! สม!!บูรณ์!!แบบ!!";
							set progress3,1;
						} else {
							mes "[Ksa]";
							mes "นี่มันเขาสัตว์โบราณ หรือเขาแมลงกันเนี่ย!";
							next;
							mes "[Ksa]";
							mes "ใช้ไม่ได้!! ไปหามาใหม่!!";
						}
					} else {
						mes "[Ksa]";
						mes "เจ้าจะหลอกข้าด้วยเขาวัวรึไง! รีบไปหาเซ่!!";
					}
					close;
				case 2:
					if (Zeny >= 30000000) {
						mes "[Ksa]";
						mes "ไหนขอนับหน่อยซิ.....";
						next;
						progressbar "0xFF0000",3;
						Zeny -= 30000000;
						mes "[Ksa]";
						mes "ข้าจะไปซื้อเขาที่ดีที่สุดมา!!";
						set progress3,1;
					} else {
						mes "[Ksa]";
						mes "นี่มันนักผจญภัยหรือขอทาน!! อย่าเอาเศษตังมาให้ข้า!!";
					}
					close;
				case 3:
					close;
			}
		}

		// เคลียร์ครบทุกขั้น
		if (progress1 == 1 && progress2 == 1 && progress3 == 1) {
			mes "[Ksa]";
			mes "เตรียมการสมบูรณ์แล้ว .... ";
			mes "เอาหละ หลังจากนี้มันต้องใช้เวลาจนกว่าท่าน Lived จะเตรียมพื้นที่วิจัยให้พร้อม!!";
			mes "งานเจ้ามันยังไม่เสร็จ!! ไปช่วยงานของพวกลูกน้องข้าแถวๆนั้นด้วย!! ไป๊!!";
			next;
			mes "[Ksa]";
			mes "เอ้า! เอานี่ไปด้วย!!";
			getitem 2002002,1;
			set sideLQA,0;
			set sideLQB,0;
			set sideLQC,0;
			set sideLQD,0;
			set step,6; //ให้ของวิจัยเสร็จ ไปเคลียเควสกับนักวิจัยอื่นๆเพิ่ม
			close;
		}
	}
	else {
		mes "[Ksa]";
		mes "อย่ามายุ่ง!! ข้ากำลังทำงานอยู่!!";
		close;
	}
	end;
}


//--------------------------------------------------
// Warp ไป Prototype (1@rgsr_d1)
//--------------------------------------------------
1@lhz_d1,151,64,4	script	Secure Gate	10237,{ // warp to dun

	if (step < 7) {
		mes "[Secure Gate]";
		mes "ห้ามเข้าก่อนได้รับอนุญาติ!!";
		close;
	}
	warp "1@rgsr_d1",126,38;
	end;
}

1@rgsr_d1,126,34,0	warp	Back_Prototype	1,1,1@lhz_d1,151,58
//----------------------------
// Warp in dungeon
//----------------------------
1@rgsr_d1,126,58,0	warp	In_Dun1	1,1,1@rgsr_d1,126,67
1@rgsr_d1,126,63,0	warp	Out_Dun1	1,1,1@rgsr_d1,126,54
1@rgsr_d1,58,135,0	warp	In_Dun2	1,1,1@rgsr_d1,66,135
1@rgsr_d1,63,135,0	warp	Out_Dun2	1,1,1@rgsr_d1,55,135
1@rgsr_d1,52,184,0	warp	In_Dun3	1,1,1@rgsr_d1,45,184
1@rgsr_d1,48,184,0	warp	Out_Dun3	1,1,1@rgsr_d1,55,184
1@rgsr_d1,48,215,0	warp	In_Dun4	1,1,1@rgsr_d1,55,215
1@rgsr_d1,52,215,0	warp	Out_Dun4	1,1,1@rgsr_d1,45,215
1@rgsr_d1,108,196,0	warp	In_Dun5	1,1,1@rgsr_d1,108,185
1@rgsr_d1,108,189,0	warp	Out_Dun5	1,1,1@rgsr_d1,108,200
1@rgsr_d1,125,161,0	warp	In_Dun6	1,1,1@rgsr_d1,125,152
1@rgsr_d1,125,156,0	warp	Out_Dun6	1,1,1@rgsr_d1,125,165
1@rgsr_d1,101,120,0	warp	In_Dun7	1,1,1@rgsr_d1,101,112
1@rgsr_d1,101,116,0	warp	Out_Dun7	1,1,1@rgsr_d1,101,124
1@rgsr_d1,144,190,0	warp	In_Dun8	1,1,1@rgsr_d1,144,198
1@rgsr_d1,144,194,0	warp	Out_Dun8	1,1,1@rgsr_d1,144,186
1@rgsr_d1,185,215,0	warp	In_Dun9	1,1,1@rgsr_d1,192,215
1@rgsr_d1,189,215,0	warp	Out_Dun9	1,1,1@rgsr_d1,181,215
1@rgsr_d1,213,136,0	warp	In_Dun10	1,1,1@rgsr_d1,221,136
1@rgsr_d1,217,136,0	warp	Out_Dun10	1,1,1@rgsr_d1,209,136
1@rgsr_d1,213,108,0	warp	In_Dun11	1,1,1@rgsr_d1,220,108
1@rgsr_d1,217,108,0	warp	Out_Dun11	1,1,1@rgsr_d1,210,108
1@rgsr_d1,213,76,0	warp	In_Dun12	1,1,1@rgsr_d1,220,76
1@rgsr_d1,217,76,0	warp	Out_Dun12	1,1,1@rgsr_d1,210,76
1@rgsr_d1,213,44,0	warp	In_Dun13	1,1,1@rgsr_d1,220,44
1@rgsr_d1,217,44,0	warp	Out_Dun13	1,1,1@rgsr_d1,210,44
1@rgsr_d1,151,120,0	warp	In_Dun14	1,1,1@rgsr_d1,151,113
1@rgsr_d1,151,116,0	warp	Out_Dun14	1,1,1@rgsr_d1,151,123
1@rgsr_d1,189,185,0	warp	In_Dun15	1,1,1@rgsr_d1,190,192
1@rgsr_d1,190,189,0	warp	Out_Dun15	1,1,1@rgsr_d1,189,182
1@rgsr_d1,236,209,0	warp	In_Dun16	1,1,1@rgsr_d1,235,202
1@rgsr_d1,235,205,0	warp	Out_Dun16	1,1,1@rgsr_d1,236,212
1@rgsr_d1,236,192,0	warp	In_Dun16	1,1,1@rgsr_d1,235,185
1@rgsr_d1,235,188,0	warp	Out_Dun16	1,1,1@rgsr_d1,236,195

//-----------------------------
// Monster + MVP
//------------------------------
//==================================================
// 1@cash - Octopus Cave
//==================================================
1@rgsr_d1,0,0	monster	Immortal Corps	27005,25,5000
1@rgsr_d1,0,0	monster	Immortal Corps	27006,25,5000
1@rgsr_d1,0,0	monster	Immortal Corps	27006,50,5000
1@rgsr_d1,200,191,5,5	boss_monster	CODE_X	27008,1,7200000,3000000,1


//=======ส่วนของนักวิทย์ A B C +อ้วน D============= //spite นักวิทย์(sideLQA,B,C) 402  อ้วน(sideLQD) 853  

//=============== sideLQA ========================//

1@lhz_d1,141,50,5	script	Scientist#Fool	402,{ // นักวิทย์ 1 -> sideLQA

    // ---------- ถ้าผ่านแล้ว ----------
	if (step != 6){
		mes "[Markus Troll]";
		mes "อย่ามายุ่ง ข้ากำลังวิจัยงานสำคัญอยู่!!";
		end;
	}
    if (#SciTalk == 1) {
        mes "[Markus Troll]";
        // แสดงชื่อตัวละครด้วย getcharid(0) ตามที่ขอ (บาง engine อาจต้อง strcharinfo เพื่อชื่อแบบ string)
        mes "สวัสดีนักผจญภัย เจ้าเคยผ่านบทเรียนนี้แล้ว จะฟังอีกก็ได้ฮ่าๆ";
        close;
    }

    // ---------- ถ้ามีโปรเกรสค้าง (หนีกลางคัน) ----------
    if (#SciTalkProg > 0 && #SciTalkProg < 20) {
        mes "[Markus Troll]";
        mes "มะกี๊เจ้าฟังข้าไม่จบนี่ งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!!";
		next;
        goto start_lecture;
    }

    // ปกติเริ่มจากตรงนี้
	mes "[Markus Troll]";
	mes "ข้าไม่ชอบพวกหัวทึบ เพราะงั้นเจ้าต้องผ่านการทดสอบความจำกับข้าซะก่อน";
    goto start_lecture;

start_lecture:
    // กำหนดว่าอยู่ในช่วงฟัง (กันคนกดออกหนี)
    set #SciTalkProg,1;
    set #SciTalk,0;
    mes "[Markus Troll]";
	mes "ข้าจะเริ่มละนะ ตั้งใจฟัง.....";
    next;

    // --------- เนื้อหา (ยาว/ลื่นไหล) ----------
    mes "[Markus Troll]";mes "เริ่มจากการชั่งมวลของสิ่งมีชีวิตต่างๆ ข้าลองชั่งโครงกระดูกมนุษย์เทียบกับ Skeleton Soldier";
	mes "โดยทั่วไปแล้วโครงกระดูกของมนุษย์ประกอบด้วยประมาณ 206 ชิ้น"; 
    mes "บางทีตัวเลขมันไม่สัมพันธ์กับความโหด เพราะบางตัวไม่มีหัวแต่ยังควบคุมการเดินได้อย่างน่ากลัว"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ข้าทดลองวัดการเต้นของหัวใจภายใต้เงื่อนไขต่างๆ พบว่าหัวใจมนุษย์เต้นเร็วเมื่อกลัว";
	mes "เลือดมนุษย์มีสีแดงเพราะมีธาตุเหล็กในฮีโมโกลบิน ซึ่งทำให้เลือดสะท้อนเป็นสีแดง";
    mes "แต่หัวใจมอนสเตอร์บางตัวกลับเต้นเมื่อได้ยินเสียงกลองรบซึ่งเรามองไม่เห็น"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }
		
        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ข้าพิสูจน์มวลสมองแบบหยาบๆ: สมองมนุษย์หนักราวหนึ่งกิโลกว่า ส่วนสมอง Orc อาจหนักกว่าเล็กน้อย";
    mes "แต่การใช้งานอาจต่างกัน — หรือพูดง่ายๆ คือ Orc อาจเลือกใช้เฉพาะฟังก์ชันที่จำเป็น"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }
		
        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "การมองเห็นก็เป็นเรื่องสนุก บางสิ่งมีเรติน่าที่ละเอียดจนนับเซลล์ได้";
    mes "บางมอนสเตอร์มี 'ตา' อยู่ทั่วตัว ซึ่งอาจทำให้มันมองได้ 360 องศา"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ข้าลองชั่งปีกทั้งๆ ที่ Poring ไม่มีปีก — บางครั้งการออกแบบร่างกายไม่จำเป็นต้องสมเหตุสมผลทางฟิสิกส์"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ลิ้นเป็นกล้ามเนื้อที่ใช้มาก มนุษย์มีลิ้นยืดหยุ่น แต่ Deviruchi ลิ้นยาวจนพันคอได้ — ข้าจดไว้เป็นเคสศึกษา"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "เลือดมีองค์ประกอบต่างกัน: มนุษย์แดง แต่ปีศาจบางชนิดมีเลือดม่วงหรือเรืองแสง";
    mes "ข้านำไปทดสอบปฏิกิริยากับแสงและกรด แต่ไม่แนะนำให้เก็บขวดเหล่านั้นกับผักผลไม้"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ข้าลองอัดเสียงกรีดร้องของ banshee แล้วพบว่าสเปกตรัมบางช่วงทำให้แก้วน้ำสั่นจนแตก"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "กระเพาะมนุษย์มี HCl ย่อยอาหารได้ แต่บางสายพันธุ์ไฟร้อนมีกรดเข้มข้นกว่า — อย่าใส่เหล็กลงไป"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "บางมอนสเตอร์ regenerating ได้เร็วมาก — ซ่อมตัวไวจนแทบไม่รู้ว่ามันขาดไป"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }
		
        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]"; mes "กล้ามเนื้อเมื่อถูกกระตุ้นด้วยไฟฟ้าจะหดตัวได้ บางสายพันธุ์เก็บพลังงานเหมือนสปริง"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "Ifrit-type มีเยื่อหุ้มกันความร้อนที่น่าทึ่ง แต่มาพร้อมการเผาผลาญสูงเหมือนแผงไฟ"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "การทดสอบอัตราการเผาผลาญเผยว่าบางชนิดเผาผลาญต่อแม้นอนนิ่ง เพราะเมตาบอลิซึมต้องเดินต่อ"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ระบบประสาทพิเศษทำให้บางเผ่าตอบสนองเร็วผิดปกติ — เหตุผลว่าทำไมบางตัวตกใจแล้วหายไปทันที"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "การประสานระหว่างการมองเห็นกับการได้ยินอาจทำให้เกิดการรับรู้ใหม่ — บางตัว 'เห็น' กลิ่น หรือ 'ได้ยิน' แสง"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "tentacle บางสายพันธุ์ยืดได้มากโดยไม่ขาดเพราะโครงสร้างเหมือนใยสังเคราะห์"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "มอนสเตอร์บางประเภทมีส่วนประกอบโลหะในร่างกาย ทำให้ตอบสนองต่อสนามแม่เหล็ก"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "ท้ายสุด ข้าทดสอบการปรับตัวในสภาพสุดโต่ง — ยีนและการฝึกช่วยให้พวกเขาปรับได้ต่างกัน"; next;

// ---- interrupt check (shuffled options, fixed) ----
    .@rnd = rand(1,100);
    if (.@rnd <= 20) {
        // เลือกคำถาม interrupt สุ่ม 1..3
        .@iq = rand(1,3);
        if (.@iq == 1) .@q2$ = "หลับยัง?";
        if (.@iq == 2) .@q2$ = "เข้าใจไหม?";
        if (.@iq == 3) .@q2$ = "เจ้าเหม่องั้นหรอ?";

        // กำหนดคำตอบ "ถูก" เป็น "ยังอยู่"
        .@correct_token = 0; // 0 = "ยังอยู่", 1 = "ห๊ะ อะไรนะ?"

        // สลับสองตัวเลือกแบบปลอดภัย (ไม่เรียก rand กับ min==max)
        // เริ่มค่า default
        .@oA$ = "ยังอยู่";
        .@oB$ = "ห๊ะ อะไรนะ?";

        // ถ้า rand(0,1) == 0 ให้สลับตำแหน่ง
        if (rand(0,1) == 0) {
            // สลับ: เอา oA -> o1 หรือ o2 ตามต้องการ
            .@o1$ = .@oB$;
            .@o2$ = .@oA$;
            .@perm0 = 1; // ถ้า o1 คือ token 1 (ห๊ะ) แทน 0
        } else {
            .@o1$ = .@oA$;
            .@o2$ = .@oB$;
            .@perm0 = 0; // ถ้า o1 คือ token 0 (ยังอยู่)
        }

        // แสดงคำถามและตัวเลือก (ตำแหน่งจะถูกสลับทุกครั้ง)
        mes "[Markus Troll]";
        mes .@q2$;
        .@ch = select(.@o1$, .@o2$); // คืนค่า 1..2
		next;
        // หาตำแหน่งคำตอบที่ถูก (1..2)
        if (.@perm0 == .@correct_token) {
            .@correct_pos = 1;
        } else {
            .@correct_pos = 2;
        }

        // ตรวจคำตอบ
        if (.@ch == .@correct_pos) {
            mes "[Markus Troll]";
            mes "ดี ข้ายังเห็นเจ้าตั้งใจอยู่ — กลับไปฟังต่อ";
            next;
        } else {
            mes "[Markus Troll]";
            mes "หืม? งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
            set #SciTalk,0;
            set #SciTalkProg,0;
            goto start_lecture;
        }
    }
    // ---- end interrupt check ----
	
    mes "[Markus Troll]";mes "เอาล่ะ ข้าหวังว่าเจ้าจะยังไม่หลับ — บอกข้ามาได้แล้วว่าเข้าใจหรือยัง?"; next;
	

    // --------- สุ่มตำแหน่งคำตอบ "เข้าใจ" / ผิด -----------
    .@understand_pos = rand(1,4);
    switch(select(
        (.@understand_pos==1?"เข้าใจแล้ว":"ไม่เข้าใจ"),
        (.@understand_pos==2?"เข้าใจแล้ว":"เล่าใหม่"),
        (.@understand_pos==3?"เข้าใจแล้ว":"งงกว่าเดิม"),
        (.@understand_pos==4?"เข้าใจแล้ว":"ขอข้ามได้มั้ย")
    )) {
        case 1:
            if (.@understand_pos==1) goto ASK_QUIZ;
            set #SciTalk,0; set #SciTalkProg,0; goto start_lecture;
        case 2:
            if (.@understand_pos==2) goto ASK_QUIZ;
            set #SciTalk,0; set #SciTalkProg,0; goto start_lecture;
        case 3:
            if (.@understand_pos==3) goto ASK_QUIZ;
            set #SciTalk,0; set #SciTalkProg,0; goto start_lecture;
        case 4:
            if (.@understand_pos==4) goto ASK_QUIZ;
            set #SciTalk,0; set #SciTalkProg,0; goto start_lecture;
    }
    end;

    // ------------------ ถามข้อสอบสุ่ม 1 ข้อ (5 ข้อ) ------------------
ASK_QUIZ:
    // เลือกคำถามแบบสุ่ม 1..5
    .@q = rand(1,5);

    // ตำแหน่งคำตอบที่ถูก (1..4) สุ่ม
    .@correct_pos = rand(1,4);

    // กำหนดคำตอบที่ถูกและคำตอบผิดสำหรับแต่ละคำถาม
    if (.@q == 1) {
        .@corr$ = "ประมาณ 206 ชิ้น";             // คำตอบถูก (ถ้าจะใช้ต้องเพิ่มบรรทัดในบทพูดตามข้อเสนอข้างบน)
        .@w1$ = "ประมาณ 150 ชิ้น";
        .@w2$ = "ประมาณ 300 ชิ้น";
        .@w3$ = "ไม่มีชิ้นเลย";
        .@qtext$ = "คำถาม: โครงกระดูกมนุษย์ปกติมีกี่ชิ้น?";
    }
    if (.@q == 2) {
        .@corr$ = "มีธาตุเหล็กในฮีโมโกลบิน";     // คำตอบถูก (ต้องมีข้อความอธิบายในบทพูดด้วยเพื่อยุติธรรม)
        .@w1$ = "มีเมทัลออร่าทางเวท";
        .@w2$ = "เพราะแสงสะท้อนเป็นสีแดง";
        .@w3$ = "เพราะกินอาหารสีแดงบ่อยๆ";
        .@qtext$ = "คำถาม: เลือดมนุษย์มีสีแดงเพราะอะไร?";
    }
    if (.@q == 3) {
        .@corr$ = "เส้นใยกล้ามเนื้อที่ยืดหยุ่น";
        .@w1$ = "กระดูกที่ยาวผิดปกติ";
        .@w2$ = "ปีกขนาดใหญ่";
        .@w3$ = "ระบบหายใจพิเศษ";
        .@qtext$ = "คำถาม: กล้ามเนื้อสามารถเก็บพลังงานเหมือนสปริงได้ ทำให้สัตว์กระโดดไกลได้จากอะไร?";
    }
    if (.@q == 4) {
        .@corr$ = "เสียงกรีดร้องของ banshee";
        .@w1$ = "เสียงหัวเราะของ Poring";
        .@w2$ = "เสียงคำรามของ Orc";
        .@w3$ = "เสียงกระซิบของ Enchantress";
        .@qtext$ = "คำถาม: ข้าทดสอบเสียงของใครแล้วแก้วน้ำสั่นจนแตก?";
    }
    if (.@q == 5) {
        .@corr$ = "กรด HCl";
        .@w1$ = "กรดซัลฟิวริก";
        .@w2$ = "น้ำตาลเข้มข้น";
        .@w3$ = "น้ำสะอาด";
        .@qtext$ = "คำถาม: กระเพาะมนุษย์ย่อยอาหารด้วยสารอะไรเป็นหลัก?";
    }

    // วางตัวเลือก 4 ตัว โดยเอาคำตอบที่ถูกไปไว้ที่ .@correct_pos และใส่ wrongs ตามลำดับในช่องว่าง
    if (.@correct_pos == 1) {
        .@o1$ = .@corr$; .@o2$ = .@w1$; .@o3$ = .@w2$; .@o4$ = .@w3$;
    }
    if (.@correct_pos == 2) {
        .@o1$ = .@w1$; .@o2$ = .@corr$; .@o3$ = .@w2$; .@o4$ = .@w3$;
    }
    if (.@correct_pos == 3) {
        .@o1$ = .@w1$; .@o2$ = .@w2$; .@o3$ = .@corr$; .@o4$ = .@w3$;
    }
    if (.@correct_pos == 4) {
        .@o1$ = .@w1$; .@o2$ = .@w2$; .@o3$ = .@w3$; .@o4$ = .@corr$;
    }

    // แสดงคำถามและตัวเลือก (ตำแหน่งถูกจะเปลี่ยนไปทุกรอบ)
    mes "[Markus Troll]";
    mes .@qtext$;
    .@choice = select(.@o1$, .@o2$, .@o3$, .@o4$);

    // ตรวจคำตอบ
    if (.@choice == .@correct_pos) {
        // ถูก
        set #SciTalk,1;
        set #SciTalkProg,20;
        set sideLQA,1;
        mes "[Markus Troll]";
        mes "ถูกต้อง! เจ้าแสดงให้เห็นว่าเข้าใจจริงๆ ยินดีด้วย!";
		mes "รับรางวัลคนเก่งไปเล้ยยยย!";
		getitem 2002002,1;
        close;
    } else {
        // ผิด -> เล่าใหม่ตั้งแต่ต้น
        mes "[Markus Troll]";
        mes "ผิด! ข้าคงอธิบายไม่ชัด งั้นข้าจะเล่าใหม่ตั้งแต่ต้น!";
		next;
        set #SciTalk,0;
        set #SciTalkProg,0;
        goto start_lecture;
    }
}

//=============== sideLQD ========================//

1@lhz_d1,157,54,5	script	Rich Man#Fool	853,{ // อ้วน -> sideLQD
	mes "[Dumble R.]";
	if (step != 6){
		mes "ใครเอาขยะมายืนจ้องหน้าข้าอยู่ตรงนี้....";
		end;
	}
	
	if (#FatRichStep == 8) {
		mes "ข้าไม่เสวนากับพวกเจ้าหรอกนะ......";
		mes "เพราะเย็นนี้ข้าเตรียมจะไปหาลูกตัวน้อยของข้า~~~~";
		close;
	}
	if (#FatRichStep == 0) {
		mes "เจ้านักผจญภัยนั่นสินะ? เหมาะเลย!";
		mes "ไปเอาของที่บ้านข้ามาให้ที!";
		next;
		select("ไม่เอาอ่ะ...","ข้าไม่อยากทำ","ข้าว่างหรือเปล่านะ?","ได้เลย...");
		mes "[Dumble R.]";
		mes "จะตอบอะไรก็ช่าง ยังไงเจ้าก็ต้องไปเอามาให้ข้า";
		mes "ของอยู่ที่บ้านพักตากอากาศของข้าในเมืองนี้..";
		mes "ข้างๆตึกตำรวจ เพราะมันปลอดภัยที่สุด!!";
		mes "อยู่ชั้น 2 นะ!! อยู่บนโต๊ะทำงานข้า!! อย่าเอามาผิดหละ!!";
		set #FatRichStep,1;
		close;
	}
	if (#FatRichStep == 1) {
		mes "ยังไม่เอาของที่บ้านข้ามาอีกหรือ?";
		mes "บ้านข้าอยู่ข้างๆตึกตำรวจ เพราะมันปลอดภัยที่สุด!!";
		mes "อยู่ชั้น 2 นะ!! อยู่บนโต๊ะทำงานข้า!! อย่าเอามาผิดหละ!!";
		close;
	}
	if (#FatRichStep == 2) {
		mes "โอเค ต่อไป... เฮ้ยเจ้าจะไปไหน!!";
		mes "ไปหยิบของอีกชิ้นที่บ้านข้าอีกรอบ!!";
		set #FatRichStep,3;
		close;
	}
	if (#FatRichStep == 3) {
		mes "ของชิ้นที่สองยังไม่ได้มาอีก?";
		mes "เจ้าตาบอดรึไง มันก็อยู่แถวๆนั้นนั่นแหละ!!";
		close;
	}
	if (#FatRichStep == 4) {
		mes "ฮ่าๆๆ ดีมาก!";
		mes "แม่ง! ข้าลืมของสำคัญไปได้ยังไงเนี่ย";
		mes "เจ้าหนะ ไปหยิบให้อีกรอบดิ๊";
		select("...","ทำไมข้าต้อง..... แม่งเอ้ยยยย");
		set #FatRichStep,5;
		close;
	}
	if (#FatRichStep == 5) {
		mes "เมื่อไหร่จะเอามาให้ข้า? ไปเอามาเดี๋ยวนี้!";
		mes "พวกนักผจญภัยหางแถวเอ้ยย.....";
		close;
	}
	if (#FatRichStep == 6) {
		mes "แหม่~มีกึ๋นเหมือนกันนี่หว่าเอ็ง";
		mes "แต่ยังไม่เสร็จ อย่า~พึ่ง~ปาย~หนาย~~~ ข้าต้องการให้เจ้าหาอีกอย่างมาให้ข้า!";
		next;
		mes "[Dumble R.]";
		mes "พรุ่งนี้วันเกิดลูกสาวข้า!! เจ้าต้องไปซื้อ Ice Cream ^i[536] จากเกาะ Jawaii มาให้ข้า 10 ชิ้น!!";
		set #FatRichStep,7;
		close;
	}
	if (#FatRichStep == 7) {
		if (countitem(536) < 10) {
			
			mes "เจ้าไปเอา Ice Cream ^i[536] มาให้ข้า 10 ชิ้นสิ!";
			mes "อย่ามาอู้ ข้าไม่อยากฟังข้อแก้ตัว!";
			close;
		} else {
			
			mes "อื้ม! นี่แหละ Ice Cream ที่เจ้าหนูน้องของข้าต้องการ!";
			next;
			delitem 536,10;
			mes "[Dumble R.]";
			mes "หมดธุระของเจ้าแล้ว!!เจ้าต้องการนี่ใช่ไหม? เอาไปแล้วจะไปไหนก็ไป!!";
			getitem 2002002,1;
			set #FatRichStep,8;
			set sideLQD,1;
			close;
		}
	}
	mes "ไปได้แล้ว!";
	close;
}

//=====================================================
// ของในบ้าน (อยู่ใน Lighthalzen บ้านข้างป้อมตำรวจ)
//=====================================================

// ชิ้นแรก
lhz_in03,97,113,5	script	Item Box#1	10249 ,{ 
	if (#FatRichStep == 1) {
		mes "เจอกล่องของDumble R....";
		mes "คุณหยิบของไปส่งให้Dumble R.";
		set #FatRichStep,2;
		close;
	}
	mes "ไม่มีอะไรน่าสนใจ";
	close;
}

// ชิ้นสอง
lhz_in03,107,102,2	script	Item Box#2	10005 ,{ 
	if (#FatRichStep == 3) {
		mes "เจอกล่องอีกใบในบ้านDumble R....";
		mes "คุณหยิบของไปส่งให้Dumble R. อีกรอบ....";
		set #FatRichStep,4;
		close;
	}
	mes "อย่าไปยุ่งกับมันเลย...";
	close;
}

// ชิ้นสาม
lhz_in03,107,89,5	script	Item Box#3	10250 ,{ 
	if (#FatRichStep == 5) {
		mes "เจอกล่องใบใหญ่ในบ้านDumble R....";
		mes "คุณหยิบของไปส่งให้Dumble R. อีกครั้ง..........";
		mes "[เห้อมมมมมมมม]";
		set #FatRichStep,6;
		close;
	}
	mes "[กล่องอะไรฟะเนี่ย....]";
	close;
}

//=========== sideLQB ============//

1@lhz_d1,147,53,4	script	Scientist#Old	923,{ // นักวิทย์ 2 -> sideLQB
	if (OldSstep == 2) {
		mes "[Max H.]";
		mes "เจ้าช่วยงานวิจัยข้าได้เยอะเลยหละ ไอ้หนุ่ม.....";
		mes "แต่ตอนนี้อย่ามารบกวนข้า ข้ากำลังดูชิ้นเนื้ออันสดใหม่อันนี้อยู่....";
		end;
	}
	if (OldSstep == 1){
		switch (isbegin_quest(107)){
			case 0: 
				mes "[Max H.]";
				mes "มันไม่น่าจะเกิดขึ้นได้นะ ฉันลืมให้เควสเจ้าหรอ....";
				setquest 107;
				end;
				
			case 1:
				switch (checkquest(107,HUNTING)){
					case 0: break;
					case 1: //เควสยังไม่เสร็จ
						mes "[Max H.]";
						mes "อืมมมม....งานวิจัยมันต้องใช้เวลานั่นแหละ....";
						break;
					case 2: //เควสเสร็จ ไปหา Lived
						mes "[Max H.]";
						mes "ยอดเยี่ยม..ยอดเยี่ยมมม.. ";
						mes "เอ็งไปช่้วยงานคนอื่นต่อได้แล้วหละ...ข้าคงยุ่งไปอีกซักพักใหญ่";
						mes "อ่ะ เอานี่ไป....";
						getitem 2002002,1;
						completequest 107;
						set OldSstep,2;
						set sideLQB,1;
						break;
					default:
						mes "Error Please contract Admin";
						end;
				}
			case 2: break;
		}
	}
	if (step == 6 && OldSstep == 0) {
		mes "[Max H.]";
		mes "ได้วัยหนุ่มสาวมาช่วยงานนี่มันดีจริงๆเลย!!";
		mes "ข้าต้องการชิ้นเนื้อพวกอมนุษย์ทั้งหลาย ที่มันแข็งแกร่ง!!และทนทาน!!";
		next;
		mes "[Max H.]";
		mes "ช่วยข้าจัดการพวกมันทีได้ไหม?";
		next;
		mes "[Max H.]";
		mes "เยี่ยม! รีบไปจัดการพวกมันแล้วกลับมาหาข้า!";
		setquest 107;
		set OldSstep,1;
		close;
	}
	end;
}
//=========== sideLQC ============//

1@lhz_d1,146,49,4	script	Scientist#Girl	982,{ // นักวิทย์ 3 -> sideLQC
	if(MiraTalk == 1 && sideLQ_count != 10){
		mes "[Dr. Mira]";
		mes "ยังจะพูดอยู่นั่นแหละ!! ไปหาได้แล้ว!!";
		mes "ข้าได้มา "+ sideLQ_count +"/10 ชุดแล้ว!!";
			end;
		}
	mes "[Dr. Mira]";
	// ต้องมี step == 6 ก่อน
	if (step != 6) {
		mes "อย่ามายุ่งกับช้านนนน เอกสารชั้นหายไปหนายยยยย!!";
		close;
	}
	// ถ้าทำเควสสำเร็จแล้ว
	if (sideLQC == 1) {
		mes "ฮ้าาา ไม่ต้องนั่งเทียนเขียนงานวิจัยใหม่แล้ว!! อุ๊บ! ชู่วววววว......";
		next;
		mes "[Dr. Mira]";
		mes "นายน่าจะเคยเจอสัตว์ทดลองของ Ksa แล้วนี่ แมวขาวดำคู่นั้นหนะ....";
		mes "ตัวป่วนสุดๆเลยขอบอก!! แถมแบ่งร่างออกมาได้อีก!!";
		next;
		mes "[Dr. Mira]";
		mes "ไม่รู้ตัวไหนกันแน่เป็นตัวจริงแล้ว นอกจาก Ksa ที่รู้....";
		close;
	}
	mes "โอยยย ทำไงดีเนี่ยยยย!!";
	next;
	mes "[Dr. Mira]";
	mes "ไอ้พวกสัตว์ทดลองตัวป่วนของ Ksa มันคาบเอาเอกสารชั้นไปเล่นอีกแล้ว!?!";
	mes "เจ้าหนะ!! มาช่วยชั้นตามหางานวิจัยเดี๋ยวนี้เลย!!";
	switch(select("เดี๋ยวจัดให้ เมืองนี้เล็กนิดเดียว!!","ได้กี่อันละนะ....","ไม่เอาอ่ะะะ")) {
	case 1:
		next;
		// เริ่มเควส: รีเซ็ตตัวแปรเอกสารทั้งหมด
		if(MiraTalk == 1){
		mes "[Dr. Mira]";
		mes "ยังจะพูดอยู่นั่นแหละ!! ไปหาได้แล้ว!!";
			end;
		}
		if(MiraTalk == 0){
		set sideLQ_doc1, 0;
		set sideLQ_doc2, 0;
		set sideLQ_doc3, 0;
		set sideLQ_doc4, 0;
		set sideLQ_doc5, 0;
		set sideLQ_doc6, 0;
		set sideLQ_doc7, 0;
		set sideLQ_doc8, 0;
		set sideLQ_doc9, 0;
		set sideLQ_doc10, 0;
		set sideLQ_count, 0;
		set sideLQC, 0;
		mes "[Dr. Mira]";
		mes "เอกสารทั้ง 10 ชุดของชั้นมันน่าจะกระจายอยู่ทั่วเมือง Lighthalzen นี่แหละ!!";
		mes "อย่าให้งานวิจัยของชั้นหายซักชุดเดียวนะ!! มันสำคัญสุดๆเลยหละ!!";
		set MiraTalk,1;
		close;
		}
	case 2:
		next;
		set .@c, sideLQ_count;
		set .@cc, sideLQ_count+10;
		mes "[Dr. Mira]";
		mes "คุณเก็บเอกสารมาได้ "+.@c+" / 10 ชุดแล้ว!!";
		close;
	case 3:
		next;
		mes "[Dr. Mira]";
		mes "เฮ้อ...ไม่น่าเสียเวลามานั่งคุยด้วยเล้ยย";
	default:
		close;
	}
}

// ===================== Document NPCs (10 ตัว) =====================
// แก้พิกัด (x,y) ของแต่ละบรรทัดตามที่คุณจะวางบนแมพ lighthalzen
// แต่ละ NPC เก็บได้ครั้งเดียวต่อผู้เล่น และจะเพิ่ม sideLQ_count ทีละ 1

// Document 1
lighthalzen,139,278,4	script	Document#01	10391,{
	set .@id, 1;
	// ถ้าทำเควสเสร็จหรือชิ้นนี้ถูกเก็บไปแล้ว ปิด
	if (sideLQC == 1 || sideLQ_doc1 == 1) {
		mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ..........";
		close;
	}
	mes "[เอกสารชิ้นที่ 1]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc1, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) {
			set sideLQC, 1;
			mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!";
		}
		close;
	} else {
		close;
	}
}

// Document 2
lighthalzen,171,215,4	script	Document#02	10391,{
	if (sideLQC == 1 || sideLQ_doc2 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 2]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc2, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 3
lighthalzen,43,314,4	script	Document#03	10391,{
	if (sideLQC == 1 || sideLQ_doc3 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 3]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc3, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 4
lighthalzen,50,81,4	script	Document#04	10391,{
	if (sideLQC == 1 || sideLQ_doc4 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 4]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc4, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 5
lighthalzen,185,163,4	script	Document#05	10391,{
	if (sideLQC == 1 || sideLQ_doc5 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 5]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc5, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 6
lighthalzen,243,152,4	script	Document#06	10391,{
	if (sideLQC == 1 || sideLQ_doc6 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 6]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc6, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 7
lighthalzen,238,269,4	script	Document#07	10391,{
	if (sideLQC == 1 || sideLQ_doc7 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 7]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc7, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 8
lighthalzen,362,312,4	script	Document#08	10391,{
	if (sideLQC == 1 || sideLQ_doc8 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 8]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc8, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 9
lighthalzen,215,42,4	script	Document#09	10391,{
	if (sideLQC == 1 || sideLQ_doc9 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 9]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc9, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}

// Document 10
lighthalzen,194,294,4	script	Document#10	10391,{
	if (sideLQC == 1 || sideLQ_doc10 == 1) { mes "ชุดนี้เคยหยิบมาแล้วนี่หว่า นี่สำเนาไว้กี่ฉบับฟะ.........."; close; }
	mes "[เอกสารชิ้นที่ 10]";
	mes "นี่มันเอกสารของ Mira นี่นา!!?";
	if (select("เก็บ","ยกเลิก") == 1) {
		set sideLQ_doc10, 1;
		set .@c, sideLQ_count;
		set .@c, .@c + 1;
		set .@cc, .@cc - 1;
		set sideLQ_count, .@c;
		mes "  ตอนนี้เก็บได้ "+.@c+" / 10";
		if (.@c >= 10) { set sideLQC, 1; mes "คุณเก็บครบ 10 ชิ้นแล้ว! เอากลับไปให้ Mira ได้แล้ว!!"; }
		close;
	} else { close; }
}


1@lhz_d1,138,56,5	script	#Exsample	2350,{ // npc blank
}
1@lhz_d1,138,56,5	script	#ExsampleP	10007,{ // npc blank
}
1@lhz_d1,138,48,5	script	#ExsamplePA	10007,{ // npc blank
}
1@lhz_d1,138,40,5	script	#ExsampleB	1195,{ // npc blank
}
1@lhz_d1,138,40,5	script	#ExsamplePB	10007,{ // npc blank
}
1@lhz_d1,164,56,4	script	#ExsampleC	2274,{ // npc blank
}
1@lhz_d1,164,56,4	script	#ExsamplePC	10007,{ // npc blank
}
1@lhz_d1,164,48,4	script	#ExsampleD	922,{ // npc blank
}
1@lhz_d1,164,48,4	script	#ExsamplePD	10007,{ // npc blank
}
1@lhz_d1,164,40,4	script	#ExsampleE	802,{ // npc blank
}
1@lhz_d1,164,40,5	script	#ExsamplePE	10007,{ // npc blank
}